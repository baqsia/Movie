import {makeStyles, shorthands} from "@fluentui/react-components";

export const movieGridStyles = makeStyles({
    title: {
        maxWidth: "170px"
    },
    releaseDate: {
        maxWidth: "120px"
    },
    rating: {
        maxWidth: "50px"
    }
});
